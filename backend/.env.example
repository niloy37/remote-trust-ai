SQLITE_PATH=remote_trust.db
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,https://piyush99755-remote-trust-ai.vercel.app/
OPENAI_API_KEY=
ADVANCED_CLASSIFIER_ARTIFACT_DIR=../ml/artifacts
ADVANCED_CLASSIFIER_REQUIRE_ARTIFACTS=false
# Baseline advanced classification is local-first and does not require paid APIs,
# live LLM calls, Neo4j, or trained artifacts for the MVP fallback path.
