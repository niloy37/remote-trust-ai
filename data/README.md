# Sample Data

`sample_jobs.json` contains eight demo postings covering verified global remote work, restricted remote roles, low-quality jobs, and scam patterns. The backend seed command analyzes these postings through the same scoring engine used by the `/analyze` endpoint.
